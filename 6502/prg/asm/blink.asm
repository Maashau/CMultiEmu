		* = $0000
		LDX		TEXT
		JSR		PRTX
		LDX		TEXT
		INX
		JSR		BLINK
		BRK
PRTX	LDA		TEXT,X
		DEX
		STA		SCRMEM,X
		CPX		#$00
		BNE		PRTX
		RTS
BLINK	LDA		#$5F
BLONK	STA		SCRMEM,X
		STX		BSTORE
		LDX		#$FF
		LDY		#$FF
DELAY	NOP
		DEY
		CPY		#0
		BNE		DELAY
		DEX
		CPX		#0
		BNE		DELAY
		LDX		BSTORE
		CMP		#$5F
		BNE		BLINK
		LDA		#$20
		JMP		BLONK
BLKRET	RTS
SCRMEM	EQU		$F690
BSTORE	.BYTE	0
TEXT	.BYTE	12
		.ASCII	"CURSOR BLINK"